<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>Paydos Studio</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Manrope:wght@500;600;700;800&family=Inter:wght@400;500;600&display=swap');

  :root{
    --bg:#0A0A0E;
    --surface:#15151C;
    --surface-alt:#1C1C25;
    --border:#2A2A34;
    --text:#F5F5F7;
    --text-dim:#93939F;
    --gold:#F5B942;
    --gold-dim:rgba(245,185,66,.14);
    --green:#3DDC84;
    --green-dim:rgba(61,220,132,.14);
    --red:#FF5C5C;
    --purple:#A78BFA;
    --blue:#5FA8FF;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;background:var(--bg);color:var(--text);
    font-family:'Inter',sans-serif;min-height:100vh;
  }
  h1,h2,h3,.display{font-family:'Manrope',sans-serif;}
  ::-webkit-scrollbar{width:8px;height:8px;}
  ::-webkit-scrollbar-thumb{background:var(--border);border-radius:4px;}

  .app{display:flex;min-height:100vh;}

  /* Sidebar */
  .sidebar{
    width:230px;flex-shrink:0;background:var(--surface);
    border-right:1px solid var(--border);
    padding:22px 16px;display:flex;flex-direction:column;
  }
  .brand{display:flex;align-items:center;gap:10px;padding:0 8px 24px;}
  .brand-mark{
    width:38px;height:38px;border-radius:10px;
    background:linear-gradient(135deg,var(--gold),#C9861F);
    display:flex;align-items:center;justify-content:center;
    font-family:'Manrope',sans-serif;font-weight:800;color:#1A1200;font-size:18px;
  }
  .brand-name{font-family:'Manrope',sans-serif;font-weight:800;font-size:15px;letter-spacing:.3px;}
  .brand-sub{font-size:9.5px;color:var(--text-dim);letter-spacing:2px;}

  .nav{display:flex;flex-direction:column;gap:2px;flex:1;}
  .nav-item{
    display:flex;align-items:center;gap:10px;
    padding:10px 12px;border-radius:9px;cursor:pointer;
    color:var(--text-dim);font-size:13.5px;font-weight:500;
    border:1px solid transparent;background:none;text-align:left;width:100%;
    font-family:inherit;
  }
  .nav-item .ic{font-size:15px;width:18px;text-align:center;}
  .nav-item:hover{color:var(--text);}
  .nav-item.active{
    background:var(--gold-dim);color:var(--gold);border-color:rgba(245,185,66,.25);font-weight:600;
  }

  .sidebar-foot{
    border:1px solid var(--border);border-radius:12px;padding:14px;margin-top:10px;
  }
  .sidebar-foot .t{font-size:12px;color:var(--text-dim);}
  .sidebar-foot .g{color:var(--gold);font-weight:700;font-size:13px;}

  /* Main */
  .main{flex:1;padding:26px 30px;max-width:1400px;}
  .topbar{
    display:flex;align-items:center;justify-content:space-between;margin-bottom:22px;
  }
  .greeting h1{margin:0;font-size:25px;font-weight:800;}
  .greeting p{margin:4px 0 0;color:var(--text-dim);font-size:13.5px;}
  .top-actions{display:flex;align-items:center;gap:14px;}
  .icon-btn{
    width:38px;height:38px;border-radius:10px;background:var(--surface);
    border:1px solid var(--border);display:flex;align-items:center;justify-content:center;
    cursor:pointer;color:var(--text-dim);font-size:15px;position:relative;
  }
  .icon-btn:hover{color:var(--text);}
  .badge{
    position:absolute;top:-4px;right:-4px;background:var(--gold);color:#1A1200;
    font-size:9.5px;font-weight:700;border-radius:8px;padding:1px 5px;
  }
  .live-chip{
    display:flex;align-items:center;gap:7px;background:var(--surface);border:1px solid var(--border);
    border-radius:10px;padding:8px 12px;font-size:12px;color:var(--text-dim);
  }
  .live-dot{width:7px;height:7px;border-radius:50%;background:var(--text-dim);}
  .live-dot.on{background:var(--green);box-shadow:0 0 0 0 rgba(61,220,132,.6);animation:pulse 1.8s infinite;}
  @keyframes pulse{0%{box-shadow:0 0 0 0 rgba(61,220,132,.55);}70%{box-shadow:0 0 0 8px rgba(61,220,132,0);}100%{box-shadow:0 0 0 0 rgba(61,220,132,0);}}

  .section{display:none;}
  .section.active{display:block;}

  .grid{display:grid;gap:16px;}
  .cols-4{grid-template-columns:repeat(4,1fr);}
  .cols-3{grid-template-columns:2fr 1.3fr 1.3fr;}
  .cols-2b{grid-template-columns:1.4fr 1fr 1fr;}
  @media(max-width:1100px){.cols-4,.cols-3,.cols-2b{grid-template-columns:repeat(2,1fr);}}
  @media(max-width:700px){.cols-4,.cols-3,.cols-2b{grid-template-columns:1fr;}}

  .card{
    background:var(--surface);border:1px solid var(--border);border-radius:16px;
    padding:18px;
  }
  .card-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;}
  .card-head h3{margin:0;font-size:14.5px;font-weight:700;}
  .card-head .link{color:var(--gold);font-size:12px;text-decoration:none;cursor:pointer;font-weight:600;}

  /* Stat cards */
  .stat-card{display:flex;flex-direction:column;gap:12px;}
  .stat-top{display:flex;align-items:center;gap:10px;}
  .stat-icon{
    width:38px;height:38px;border-radius:10px;background:var(--gold-dim);color:var(--gold);
    display:flex;align-items:center;justify-content:center;font-size:16px;
  }
  .stat-label{font-size:12.5px;color:var(--text-dim);font-weight:500;}
  .stat-value{font-size:24px;font-weight:800;font-family:'Manrope',sans-serif;}
  .stat-delta{font-size:11.5px;font-weight:600;}
  .stat-delta.up{color:var(--green);}
  .stat-delta.flat{color:var(--text-dim);}

  /* Progress */
  .progress-track{
    display:flex;gap:4px;margin:14px 0 12px;
  }
  .progress-seg{flex:1;height:16px;border-radius:4px;background:var(--surface-alt);}
  .progress-seg.filled{background:linear-gradient(135deg,var(--gold),#C9861F);}
  .bar-track{height:8px;border-radius:5px;background:var(--surface-alt);overflow:hidden;margin:6px 0 12px;}
  .bar-fill{height:100%;border-radius:5px;background:linear-gradient(90deg,var(--gold),#C9861F);}

  .goal-row{margin-bottom:14px;}
  .goal-row:last-child{margin-bottom:0;}
  .goal-top{display:flex;align-items:center;justify-content:space-between;font-size:12.5px;margin-bottom:2px;}
  .goal-name{display:flex;align-items:center;gap:7px;color:var(--text);font-weight:500;}
  .goal-nums{color:var(--text-dim);font-family:'Manrope',sans-serif;font-weight:700;font-size:12px;}

  /* Tasks */
  .task-row{display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--border);}
  .task-row:last-child{border-bottom:none;}
  .task-check{
    width:17px;height:17px;border-radius:5px;border:1.5px solid var(--border);flex-shrink:0;cursor:pointer;
    display:flex;align-items:center;justify-content:center;font-size:11px;color:var(--bg);
  }
  .task-check.done{background:var(--green);border-color:var(--green);}
  .task-text{flex:1;font-size:12.5px;}
  .task-text.done{text-decoration:line-through;color:var(--text-dim);}
  .task-tag{font-size:10px;color:var(--text-dim);font-family:'Manrope',sans-serif;}
  .task-del{background:none;border:none;color:var(--text-dim);cursor:pointer;font-size:13px;}
  .task-del:hover{color:var(--red);}
  .add-row{display:flex;gap:8px;margin-bottom:12px;}
  .add-row input,.add-row select{
    background:var(--surface-alt);border:1px solid var(--border);color:var(--text);
    border-radius:8px;padding:8px 10px;font-size:12.5px;font-family:inherit;
  }
  .add-row input{flex:1;}
  .btn-gold{
    background:var(--gold);color:#1A1200;border:none;border-radius:8px;
    padding:8px 14px;font-weight:700;font-size:12.5px;cursor:pointer;font-family:inherit;
  }
  .btn-gold:hover{filter:brightness(1.08);}
  .btn-ghost{
    background:var(--surface-alt);color:var(--text);border:1px solid var(--border);border-radius:8px;
    padding:8px 14px;font-size:12.5px;cursor:pointer;font-family:inherit;
  }
  .btn-ghost:hover{border-color:var(--gold);}

  /* Video rows */
  .video-row{display:flex;gap:12px;align-items:center;padding:10px 0;border-bottom:1px solid var(--border);}
  .video-row:last-child{border-bottom:none;}
  .video-thumb{width:74px;height:42px;border-radius:8px;object-fit:cover;background:var(--surface-alt);flex-shrink:0;}
  .video-title{font-size:13px;font-weight:600;margin-bottom:4px;}
  .video-meta{font-size:11px;color:var(--text-dim);display:flex;gap:10px;}
  .status-pill{
    background:var(--green-dim);color:var(--green);font-size:9.5px;font-weight:700;
    padding:2px 8px;border-radius:20px;letter-spacing:.3px;
  }
  .video-time{font-size:11px;color:var(--text-dim);white-space:nowrap;}

  /* Bars chart */
  .bar-chart{display:flex;align-items:flex-end;gap:10px;height:150px;padding-top:10px;}
  .bar-chart .b{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px;}
  .bar-chart .b .fill{width:100%;border-radius:6px 6px 0 0;background:linear-gradient(180deg,var(--gold),#C9861F);}
  .bar-chart .b .lbl{font-size:9.5px;color:var(--text-dim);text-align:center;max-width:70px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
  .bar-chart .b .val{font-size:10.5px;font-weight:700;color:var(--text);}

  /* Mini calendar */
  .mini-week{display:grid;grid-template-columns:repeat(7,1fr);gap:6px;text-align:center;margin-bottom:12px;}
  .mini-day-label{font-size:9.5px;color:var(--text-dim);}
  .mini-day{
    padding:8px 2px;border-radius:8px;font-size:12px;font-weight:600;border:1px solid transparent;
    cursor:pointer;position:relative;
  }
  .mini-day.today{background:var(--gold);color:#1A1200;}
  .mini-day.has-plan:not(.today){border-color:var(--gold);color:var(--gold);}
  .plan-list .plan-item{display:flex;justify-content:space-between;font-size:12px;padding:7px 0;border-bottom:1px solid var(--border);}
  .plan-list .plan-item:last-child{border-bottom:none;}
  .plan-day{color:var(--text-dim);font-family:'Manrope',sans-serif;font-weight:700;font-size:11px;}

  /* Pipeline */
  .pipeline{display:grid;grid-template-columns:repeat(6,1fr);gap:12px;}
  @media(max-width:1200px){.pipeline{grid-template-columns:repeat(3,1fr);}}
  @media(max-width:700px){.pipeline{grid-template-columns:repeat(2,1fr);}}
  .pl-col{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:12px;min-height:120px;}
  .pl-col-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;}
  .pl-col-head .dot{width:8px;height:8px;border-radius:50%;}
  .pl-col-title{font-size:12px;font-weight:700;display:flex;align-items:center;gap:7px;}
  .pl-count{font-size:10.5px;color:var(--text-dim);background:var(--surface-alt);padding:1px 7px;border-radius:10px;}
  .pl-card{
    background:var(--surface-alt);border:1px solid var(--border);border-radius:9px;
    padding:9px 10px;margin-bottom:8px;font-size:12px;
  }
  .pl-card-actions{display:flex;justify-content:space-between;align-items:center;margin-top:7px;}
  .pl-move{background:none;border:none;color:var(--gold);font-size:11px;cursor:pointer;padding:0;font-weight:600;}
  .pl-del{background:none;border:none;color:var(--text-dim);cursor:pointer;font-size:12px;}
  .pl-del:hover{color:var(--red);}
  .pl-add{width:100%;background:none;border:1px dashed var(--border);color:var(--text-dim);
    border-radius:8px;padding:7px;font-size:11.5px;cursor:pointer;font-family:inherit;}
  .pl-add:hover{border-color:var(--gold);color:var(--gold);}

  /* Simple list (sponsors/wishlist) */
  .simple-table{width:100%;border-collapse:collapse;}
  .simple-table th{
    text-align:left;font-size:11px;color:var(--text-dim);font-weight:600;
    padding:8px 10px;border-bottom:1px solid var(--border);
  }
  .simple-table td{padding:10px;border-bottom:1px solid var(--border);font-size:12.5px;}
  .simple-table tr:last-child td{border-bottom:none;}
  select.mini-select{
    background:var(--surface-alt);border:1px solid var(--border);color:var(--text);
    border-radius:7px;padding:5px 8px;font-size:11.5px;font-family:inherit;
  }

  .empty{color:var(--text-dim);font-size:12.5px;text-align:center;padding:24px 10px;border:1px dashed var(--border);border-radius:10px;}

  /* Chat */
  .msg{max-width:88%;padding:10px 13px;border-radius:12px;font-size:12.5px;line-height:1.5;}
  .msg.user{align-self:flex-end;background:var(--gold-dim);color:var(--gold);border:1px solid rgba(245,185,66,.25);}
  .msg.ai{align-self:flex-start;background:var(--surface-alt);border:1px solid var(--border);}
  .msg.ai.loading{color:var(--text-dim);font-style:italic;}
  .msg-add{
    display:inline-block;margin-top:8px;background:none;border:1px solid var(--gold);color:var(--gold);
    border-radius:7px;padding:4px 10px;font-size:11px;cursor:pointer;font-weight:600;font-family:inherit;
  }
  .msg-add:hover{background:var(--gold-dim);}

  /* Modal */
  .modal-backdrop{position:fixed;inset:0;background:rgba(5,5,8,.72);display:none;align-items:center;justify-content:center;z-index:60;}
  .modal-backdrop.open{display:flex;}
  .modal{background:var(--surface);border:1px solid var(--border);border-radius:14px;width:min(420px,90vw);padding:22px;}
  .modal h3{margin:0 0 4px;}
  .modal p{color:var(--text-dim);font-size:12.5px;margin-top:0;line-height:1.5;}
  .modal label{font-size:12px;color:var(--text-dim);display:block;margin:12px 0 5px;}
  .modal input{width:100%;background:var(--surface-alt);border:1px solid var(--border);color:var(--text);
    padding:9px 11px;border-radius:8px;font-family:'Manrope',sans-serif;font-size:12.5px;}
  .modal .row{display:flex;gap:8px;margin-top:20px;}
  .modal .row .btn-ghost,.modal .row .btn-gold{flex:1;text-align:center;}
  .status-msg{font-size:12px;margin-top:10px;min-height:16px;}
  .status-msg.err{color:var(--red);}

  /* Full calendar (Takvim page) */
  .cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:6px;}
  .cal-day-label{font-size:10px;color:var(--text-dim);text-align:center;font-family:'Manrope',sans-serif;}
  .cal-cell{min-height:70px;border:1px solid var(--border);border-radius:8px;padding:6px;font-size:11px;background:var(--surface-alt);cursor:pointer;}
  .cal-cell:hover{border-color:var(--gold);}
  .cal-cell.today{border-color:var(--gold);}
  .cal-cell.muted{opacity:.3;}
  .cal-plan{margin-top:4px;background:var(--gold-dim);color:var(--gold);border-radius:5px;padding:3px 5px;font-size:10px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
</style>
</head>
<body>

<div class="app">
  <div class="sidebar">
    <div class="brand">
      <div class="brand-mark">P</div>
      <div>
        <div class="brand-name" id="brandName">PAYDOS</div>
        <div class="brand-sub">STUDIO</div>
      </div>
    </div>
    <div class="nav">
      <button class="nav-item active" data-section="dashboard"><span class="ic">🏠</span>Dashboard</button>
      <button class="nav-item" data-section="icerikler"><span class="ic">🎬</span>İçerikler</button>
      <button class="nav-item" data-section="takvim"><span class="ic">📅</span>Takvim</button>
      <button class="nav-item" data-section="pipeline"><span class="ic">💡</span>Fikir Havuzu</button>
      <button class="nav-item" data-section="analytics"><span class="ic">📊</span>Analytics</button>
      <button class="nav-item" data-section="sponsorlar"><span class="ic">🤝</span>Sponsorlar</button>
      <button class="nav-item" data-section="wishlist"><span class="ic">🛒</span>Wishlist</button>
      <button class="nav-item" data-section="ayarlar"><span class="ic">⚙️</span>Ayarlar</button>
    </div>
    <div class="sidebar-foot">
      <div class="t">Video Hedefi</div>
      <div class="g" id="footChallenge">0 / 100</div>
    </div>
  </div>

  <div class="main">
    <div class="topbar">
      <div class="greeting">
        <h1 id="greetTitle">Merhaba 👋</h1>
        <p>Bugün harika içerikler üretmek için güzel bir gün.</p>
      </div>
      <div class="top-actions">
        <div class="live-chip"><span class="live-dot" id="liveDot"></span><span id="liveText">Bağlı değil</span></div>
        <div class="icon-btn" id="settingsBtn">⚙️</div>
      </div>
    </div>

    <!-- DASHBOARD -->
    <div class="section active" id="sec-dashboard">
      <div class="grid cols-4" style="margin-bottom:16px;">
        <div class="card stat-card">
          <div class="stat-top"><div class="stat-icon">▶</div><div class="stat-label">Toplam İzlenme</div></div>
          <div class="stat-value" id="statViews">—</div>
          <div class="stat-delta flat" id="statViewsDelta">Bağlan ve gör</div>
        </div>
        <div class="card stat-card">
          <div class="stat-top"><div class="stat-icon">👥</div><div class="stat-label">Abone</div></div>
          <div class="stat-value" id="statSubs">—</div>
          <div class="stat-delta flat">Toplam</div>
        </div>
        <div class="card stat-card">
          <div class="stat-top"><div class="stat-icon">📈</div><div class="stat-label">Video Başına Ort.</div></div>
          <div class="stat-value" id="statAvg">—</div>
          <div class="stat-delta flat">izlenme/video</div>
        </div>
        <div class="card stat-card">
          <div class="stat-top"><div class="stat-icon">🎬</div><div class="stat-label">Toplam Video</div></div>
          <div class="stat-value" id="statVideos">—</div>
          <div class="stat-delta flat">yüklendi</div>
        </div>
      </div>

      <div class="grid cols-3" style="margin-bottom:16px;">
        <div class="card">
          <div class="card-head"><h3>Video Hedefi</h3><span class="link" id="editGoalTarget">Hedefi Düzenle</span></div>
          <div class="progress-track" id="challengeTrack"></div>
          <div style="font-size:12px;color:var(--text-dim);" id="challengeText">Devam et 💪</div>
        </div>
        <div class="card">
          <div class="card-head"><h3>Bu Ayki Hedefler</h3></div>
          <div id="goalsBody"></div>
        </div>
        <div class="card">
          <div class="card-head"><h3>Bugünkü Görevler</h3><span class="link" id="taskCountLbl"></span></div>
          <div class="add-row">
            <input type="text" id="taskInput" placeholder="Yeni görev...">
            <button class="btn-gold" id="taskAddBtn">+</button>
          </div>
          <div id="tasksBody"></div>
        </div>
      </div>

      <div class="grid cols-2b" style="margin-bottom:16px;">
        <div class="card">
          <div class="card-head"><h3>Son Videolar</h3><span class="link" data-goto="icerikler">Tümünü Gör →</span></div>
          <div id="dashVideos"><div class="empty">Kanal bağlanınca son videoların burada listelenecek.</div></div>
        </div>
        <div class="card">
          <div class="card-head"><h3>Video Performansı</h3></div>
          <div id="dashChart"><div class="empty">Veri için kanalını bağla.</div></div>
        </div>
        <div class="card">
          <div class="card-head"><h3>Takvim</h3><span class="link" data-goto="takvim">Tüm Takvim →</span></div>
          <div class="mini-week" id="miniWeekGrid"></div>
          <div class="plan-list" id="miniWeekPlans"></div>
        </div>
      </div>

      <div class="card">
        <div class="card-head"><h3>İçerik Pipeline</h3><span class="link" data-goto="pipeline">Panoyu Aç →</span></div>
        <div class="pipeline" id="dashPipeline"></div>
      </div>
    </div>

    <!-- İÇERİKLER -->
    <div class="section" id="sec-icerikler">
      <div class="card">
        <div class="card-head"><h3>Tüm Videolar</h3><span class="link" id="refreshVideosBtn">↻ Yenile</span></div>
        <div id="fullVideosBody"><div class="empty">Kanal bağlanınca videoların burada listelenecek.</div></div>
      </div>
    </div>

    <!-- TAKVİM -->
    <div class="section" id="sec-takvim">
      <div class="card">
        <div class="card-head">
          <button class="btn-ghost" id="calPrev">‹</button>
          <h3 id="calMonthLabel" style="font-size:16px;"></h3>
          <button class="btn-ghost" id="calNext">›</button>
        </div>
        <div class="cal-grid" id="calGrid"></div>
      </div>
    </div>

    <!-- FİKİR HAVUZU / PIPELINE -->
    <div class="section" id="sec-pipeline">
      <div class="grid" style="grid-template-columns:1.3fr 1fr;margin-bottom:16px;" id="pipelineTopGrid">
        <div class="card" style="display:flex;flex-direction:column;height:520px;">
          <div class="card-head"><h3>🤖 Fikir Asistanı</h3><span class="link" id="chatClearBtn">Sohbeti Temizle</span></div>
          <div id="chatMessages" style="flex:1;overflow-y:auto;display:flex;flex-direction:column;gap:10px;padding-right:4px;"></div>
          <div style="margin-top:12px;">
            <div class="add-row" style="margin-bottom:0;">
              <input type="text" id="chatInput" placeholder="Kanalın için bir video fikri sor, tartış...">
              <button class="btn-gold" id="chatSendBtn">Gönder</button>
            </div>
          </div>
        </div>
        <div class="card">
          <div class="card-head"><h3>Nasıl kullanılır?</h3></div>
          <div style="font-size:12.5px;color:var(--text-dim);line-height:1.7;">
            Asistana kanalın, hedef kitlen ve son videoların hakkında sorular sorabilirsin. Beğendiğin bir fikri
            <b style="color:var(--gold);">"Panoya Ekle"</b> butonuyla direkt <b>Fikir</b> sütununa kart olarak ekleyebilirsin.
            <br><br>
            Örnek: <i>"Son videolarıma bakarak 5 yeni Shorts fikri öner"</i> veya <i>"İzlenmeyi artıracak bir başlık formülü bul"</i>.
          </div>
        </div>
      </div>
      <div class="card">
        <div class="card-head"><h3>İçerik Pipeline Panosu</h3></div>
        <div class="pipeline" id="fullPipeline"></div>
      </div>
    </div>

    <!-- ANALYTICS -->
    <div class="section" id="sec-analytics">
      <div class="card" style="margin-bottom:16px;">
        <div class="card-head">
          <h3>Gerçek Zamanlı Analytics</h3>
          <span class="link" id="analyticsConnectBtn">🔗 Analytics'e Bağlan</span>
        </div>
        <div id="analyticsAuthStatus" style="font-size:12px;color:var(--text-dim);">Henüz bağlı değil — Ayarlar sayfasından OAuth Client ID'ni girip buradan bağlanabilirsin.</div>
        <div class="grid cols-4" id="analyticsRealStats" style="margin-top:14px;display:none;">
          <div class="card stat-card"><div class="stat-label">İzlenme (Son 30 Gün)</div><div class="stat-value" id="anViews30">—</div></div>
          <div class="card stat-card"><div class="stat-label">İzlenme Süresi (dk)</div><div class="stat-value" id="anMinutes30">—</div></div>
          <div class="card stat-card"><div class="stat-label">Kazanılan Abone</div><div class="stat-value" id="anSubsGained">—</div></div>
          <div class="card stat-card"><div class="stat-label">Ort. İzleme Süresi</div><div class="stat-value" id="anAvgDuration">—</div></div>
        </div>
        <div id="analyticsTrafficBody" style="margin-top:14px;"></div>
      </div>
      <div class="card">
        <div class="card-head"><h3>Son Videolar — İzlenme / Beğeni / Yorum</h3></div>
        <div id="analyticsChart"><div class="empty">Kanal bağlanınca analiz burada görünecek.</div></div>
      </div>
    </div>

    <!-- SPONSORLAR -->
    <div class="section" id="sec-sponsorlar">
      <div class="card">
        <div class="card-head"><h3>Sponsorlar</h3></div>
        <div class="add-row">
          <input type="text" id="sponsorInput" placeholder="Sponsor / marka adı...">
          <select id="sponsorStatus">
            <option value="Görüşülüyor">Görüşülüyor</option>
            <option value="Anlaşıldı">Anlaşıldı</option>
            <option value="Yayında">Yayında</option>
          </select>
          <button class="btn-gold" id="sponsorAddBtn">Ekle</button>
        </div>
        <table class="simple-table" id="sponsorTable"><tbody></tbody></table>
      </div>
    </div>

    <!-- WISHLIST -->
    <div class="section" id="sec-wishlist">
      <div class="card">
        <div class="card-head"><h3>Wishlist</h3></div>
        <div class="add-row">
          <input type="text" id="wishInput" placeholder="Ekipman / ürün adı...">
          <button class="btn-gold" id="wishAddBtn">Ekle</button>
        </div>
        <table class="simple-table" id="wishTable"><tbody></tbody></table>
      </div>
    </div>

    <!-- AYARLAR -->
    <div class="section" id="sec-ayarlar">
      <div class="card" style="max-width:460px;">
        <div class="card-head"><h3>Kanal Bağlantısı</h3></div>
        <p style="color:var(--text-dim);font-size:12.5px;">YouTube Data API v3 için ücretsiz bir API anahtarı gerekir. Bilgiler yalnızca sana özel olarak saklanır.</p>
        <label style="font-size:12px;color:var(--text-dim);display:block;margin:12px 0 5px;">API Anahtarı</label>
        <input type="text" id="apiKeyInput" placeholder="AIza..." style="width:100%;background:var(--surface-alt);border:1px solid var(--border);color:var(--text);padding:9px 11px;border-radius:8px;font-size:12.5px;">
        <label style="font-size:12px;color:var(--text-dim);display:block;margin:12px 0 5px;">Kanal ID veya @kullaniciadi</label>
        <input type="text" id="channelIdInput" placeholder="UCxxxxxx veya @kanaladi" style="width:100%;background:var(--surface-alt);border:1px solid var(--border);color:var(--text);padding:9px 11px;border-radius:8px;font-size:12.5px;">
        <div class="status-msg" id="modalStatus"></div>
        <div class="row" style="margin-top:16px;display:flex;gap:8px;">
          <button class="btn-gold" id="modalSave" style="flex:1;">Bağlan ve Kaydet</button>
        </div>
      </div>

      <div class="card" style="max-width:460px;margin-top:16px;">
        <div class="card-head"><h3>Analytics Bağlantısı (OAuth)</h3></div>
        <p style="color:var(--text-dim);font-size:12.5px;">
          Gerçek izlenme süresi ve trafik kaynağı verileri için Google OAuth Client ID gerekir. Bu, sayfanın
          <b>http/https</b> bir adreste (file:// değil) barındırılmasını gerektirir — Google'ın güvenlik kuralı.
          Google Cloud Console → Credentials → Create Credentials → OAuth client ID → "Web application" seçip,
          bu sayfayı barındırdığın adresi hem "Authorized JavaScript origins" hem de yönlendirme olarak eklemen yeterli.
        </p>
        <label style="font-size:12px;color:var(--text-dim);display:block;margin:12px 0 5px;">OAuth Client ID</label>
        <input type="text" id="oauthClientIdInput" placeholder="xxxx.apps.googleusercontent.com" style="width:100%;background:var(--surface-alt);border:1px solid var(--border);color:var(--text);padding:9px 11px;border-radius:8px;font-size:12.5px;">
        <div class="row" style="margin-top:16px;display:flex;gap:8px;">
          <button class="btn-gold" id="saveOauthBtn" style="flex:1;">Kaydet</button>
        </div>
        <div class="status-msg" id="oauthStatus"></div>
      </div>
    </div>

  </div>
</div>

<div class="modal-backdrop" id="modalBackdrop">
  <div class="modal">
    <h3>Kanal Bağlantısı</h3>
    <p>YouTube Data API v3 için ücretsiz bir API anahtarı gerekir. Bilgiler yalnızca sana özel olarak saklanır.</p>
    <label>API Anahtarı</label>
    <input type="text" id="apiKeyInputModal" placeholder="AIza...">
    <label>Kanal ID veya @kullaniciadi</label>
    <input type="text" id="channelIdInputModal" placeholder="UCxxxxxx veya @kanaladi">
    <div class="status-msg" id="modalStatus2"></div>
    <div class="row">
      <button class="btn-ghost" id="modalCancel">Vazgeç</button>
      <button class="btn-gold" id="modalSave2">Bağlan ve Kaydet</button>
    </div>
  </div>
</div>

<script src="https://accounts.google.com/gsi/client" async defer></script>
<script>
const PIPELINE_STAGES = [
  {key:'fikir', label:'Fikir', color:'#F5B942'},
  {key:'senaryo', label:'Senaryo', color:'#5FA8FF'},
  {key:'cekim', label:'Çekim', color:'#A78BFA'},
  {key:'montaj', label:'Montaj', color:'#FF8A5C'},
  {key:'kapak', label:'Kapak', color:'#3DDC84'},
  {key:'yayinda', label:'Yayında', color:'#9A9AA5'},
];

/* Claude önizlemesinde window.storage, gerçek sitede localStorage kullanır */
async function storageGet(key){
  if(typeof window.storage !== 'undefined'){
    try{ return await window.storage.get(key, false); }catch(e){ return null; }
  }
  const v = localStorage.getItem(key);
  return v!==null ? {value:v} : null;
}
async function storageSet(key, value){
  if(typeof window.storage !== 'undefined'){
    try{ return await window.storage.set(key, value, false); }catch(e){ return null; }
  }
  try{ localStorage.setItem(key, value); }catch(e){}
  return {key, value};
}

function fmt(n){
  n = Number(n)||0;
  if(n>=1000000) return (n/1000000).toFixed(1).replace(/\.0$/,'')+'M';
  if(n>=1000) return (n/1000).toFixed(1).replace(/\.0$/,'')+'B';
  return n.toLocaleString('tr-TR');
}
function escapeHtml(s){ const d=document.createElement('div'); d.textContent=s; return d.innerHTML; }
function timeAgo(dateStr){
  const diff = (Date.now()-new Date(dateStr).getTime())/1000;
  if(diff<3600) return Math.floor(diff/60)+' dk önce';
  if(diff<86400) return Math.floor(diff/3600)+' saat önce';
  if(diff<2592000) return Math.floor(diff/86400)+' gün önce';
  return Math.floor(diff/2592000)+' ay önce';
}

/* ---------- Navigation ---------- */
document.querySelectorAll('.nav-item').forEach(btn=>{
  btn.onclick = ()=> switchSection(btn.dataset.section);
});
document.querySelectorAll('[data-goto]').forEach(el=>{
  el.onclick = ()=> switchSection(el.dataset.goto);
});
function switchSection(name){
  document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active', b.dataset.section===name));
  document.querySelectorAll('.section').forEach(s=>s.classList.toggle('active', s.id==='sec-'+name));
}

/* ---------- Settings / API ---------- */
let settings = {apiKey:'', channelId:''};
const modalBackdrop = document.getElementById('modalBackdrop');
document.getElementById('settingsBtn').onclick = ()=>{
  document.getElementById('apiKeyInputModal').value = settings.apiKey||'';
  document.getElementById('channelIdInputModal').value = settings.channelId||'';
  modalBackdrop.classList.add('open');
};
document.getElementById('modalCancel').onclick = ()=> modalBackdrop.classList.remove('open');

async function loadSettings(){
  try{
    const r = await storageGet('yt-settings', false);
    if(r && r.value){
      settings = JSON.parse(r.value);
      document.getElementById('apiKeyInput').value = settings.apiKey||'';
      document.getElementById('channelIdInput').value = settings.channelId||'';
      if(settings.apiKey && settings.channelId) fetchChannelData();
    }
  }catch(e){}
}
async function saveSettings(){
  try{ await storageSet('yt-settings', JSON.stringify(settings), false); }catch(e){}
}
async function doSave(apiKey, channelId, statusEl){
  if(!apiKey || !channelId){
    statusEl.textContent='Lütfen her iki alanı da doldur.'; statusEl.className='status-msg err'; return false;
  }
  settings = {apiKey, channelId};
  await saveSettings();
  document.getElementById('apiKeyInput').value = apiKey;
  document.getElementById('channelIdInput').value = channelId;
  await fetchChannelData();
  return true;
}
document.getElementById('modalSave').onclick = ()=> doSave(
  document.getElementById('apiKeyInput').value.trim(),
  document.getElementById('channelIdInput').value.trim(),
  document.getElementById('modalStatus')
);
document.getElementById('modalSave2').onclick = async ()=>{
  const ok = await doSave(
    document.getElementById('apiKeyInputModal').value.trim(),
    document.getElementById('channelIdInputModal').value.trim(),
    document.getElementById('modalStatus2')
  );
  if(ok) modalBackdrop.classList.remove('open');
};

let lastVideos = [];

async function fetchChannelData(){
  const {apiKey, channelId} = settings;
  if(!apiKey || !channelId) return;
  document.getElementById('liveText').textContent = 'Bağlanıyor...';

  try{
    let url = channelId.startsWith('@')
      ? `https://www.googleapis.com/youtube/v3/channels?part=snippet,statistics,contentDetails&forHandle=${encodeURIComponent(channelId)}&key=${apiKey}`
      : `https://www.googleapis.com/youtube/v3/channels?part=snippet,statistics,contentDetails&id=${encodeURIComponent(channelId)}&key=${apiKey}`;
    const res = await fetch(url);
    const data = await res.json();
    if(data.error) throw new Error(data.error.message||'API hatası');
    if(!data.items || !data.items.length) throw new Error('Kanal bulunamadı.');

    const chan = data.items[0];
    document.getElementById('brandName').textContent = chan.snippet.title.toUpperCase().slice(0,14);
    document.getElementById('greetTitle').textContent = 'Merhaba, ' + chan.snippet.title + ' 👋';
    document.getElementById('liveDot').classList.add('on');
    document.getElementById('liveText').textContent = 'Bağlı · ' + new Date().toLocaleTimeString('tr-TR');

    const st = chan.statistics;
    document.getElementById('statViews').textContent = fmt(st.viewCount);
    document.getElementById('statSubs').textContent = fmt(st.subscriberCount);
    document.getElementById('statVideos').textContent = fmt(st.videoCount);
    document.getElementById('statAvg').textContent = fmt(Math.round((st.viewCount||0)/(st.videoCount||1)));
    document.getElementById('statViewsDelta').textContent = 'Kanal geneli';

    renderChallenge(Number(st.videoCount)||0);

    const uploadsPlaylistId = chan.contentDetails.relatedPlaylists.uploads;
    await fetchVideos(uploadsPlaylistId, apiKey);

  }catch(err){
    document.getElementById('liveText').textContent = 'Bağlantı hatası';
    document.getElementById('liveDot').classList.remove('on');
    const s2 = document.getElementById('modalStatus2');
    s2.textContent = err.message; s2.className='status-msg err';
  }
}

async function fetchVideos(playlistId, apiKey){
  try{
    const plUrl = `https://www.googleapis.com/youtube/v3/playlistItems?part=snippet,contentDetails&maxResults=15&playlistId=${playlistId}&key=${apiKey}`;
    const plRes = await fetch(plUrl);
    const plData = await plRes.json();
    if(plData.error) throw new Error(plData.error.message);
    const ids = plData.items.map(i=>i.contentDetails.videoId).join(',');
    if(!ids){
      document.getElementById('dashVideos').innerHTML = '<div class="empty">Henüz video yok.</div>';
      document.getElementById('fullVideosBody').innerHTML = '<div class="empty">Henüz video yok.</div>';
      return;
    }
    const vUrl = `https://www.googleapis.com/youtube/v3/videos?part=snippet,statistics&id=${ids}&key=${apiKey}`;
    const vRes = await fetch(vUrl);
    const vData = await vRes.json();
    if(vData.error) throw new Error(vData.error.message);
    lastVideos = vData.items;
    renderVideos();
    renderChart();
  }catch(err){
    document.getElementById('dashVideos').innerHTML = `<div class="empty">Video listesi alınamadı: ${err.message}</div>`;
  }
}

function renderVideos(){
  const rowHtml = v => `
    <div class="video-row">
      <img class="video-thumb" src="${v.snippet.thumbnails.default.url}" alt="">
      <div style="flex:1;min-width:0;">
        <div class="video-title">${escapeHtml(v.snippet.title)}</div>
        <div class="video-meta"><span>▶ ${fmt(v.statistics.viewCount)}</span><span>👍 ${fmt(v.statistics.likeCount)}</span><span>💬 ${fmt(v.statistics.commentCount)}</span></div>
      </div>
      <div class="video-time">${timeAgo(v.snippet.publishedAt)}</div>
    </div>`;
  document.getElementById('dashVideos').innerHTML = lastVideos.slice(0,4).map(rowHtml).join('') || '<div class="empty">Video yok.</div>';
  document.getElementById('fullVideosBody').innerHTML = lastVideos.map(rowHtml).join('') || '<div class="empty">Video yok.</div>';
}

function renderChart(){
  const top = lastVideos.slice(0,6);
  if(!top.length){ return; }
  const max = Math.max(...top.map(v=>Number(v.statistics.viewCount)||0), 1);
  const barHtml = (v)=>{
    const views = Number(v.statistics.viewCount)||0;
    const h = Math.max(6, Math.round((views/max)*130));
    return `<div class="b"><div class="val">${fmt(views)}</div><div class="fill" style="height:${h}px;"></div><div class="lbl" title="${escapeHtml(v.snippet.title)}">${escapeHtml(v.snippet.title)}</div></div>`;
  };
  document.getElementById('dashChart').innerHTML = `<div class="bar-chart">${top.map(barHtml).join('')}</div>`;

  document.getElementById('analyticsChart').innerHTML = `
    <table class="simple-table"><thead><tr><th>Video</th><th>İzlenme</th><th>Beğeni</th><th>Yorum</th><th>Yayın</th></tr></thead>
    <tbody>${lastVideos.map(v=>`
      <tr><td>${escapeHtml(v.snippet.title)}</td><td>${fmt(v.statistics.viewCount)}</td><td>${fmt(v.statistics.likeCount)}</td><td>${fmt(v.statistics.commentCount)}</td><td>${timeAgo(v.snippet.publishedAt)}</td></tr>
    `).join('')}</tbody></table>`;
}
document.getElementById('refreshVideosBtn').onclick = ()=> fetchChannelData();

/* ---------- Video Hedefi (Challenge) ---------- */
let challengeTarget = 100;
async function loadChallenge(){
  try{
    const r = await storageGet('yt-goal-target', false);
    if(r && r.value) challengeTarget = Number(r.value)||100;
  }catch(e){}
  renderChallenge(Number((document.getElementById('statVideos').textContent||'0').replace(/\D/g,''))||0);
}
function renderChallenge(current){
  const segCount = 20;
  const filledSeg = Math.round((current/challengeTarget)*segCount);
  let html='';
  for(let i=0;i<segCount;i++) html += `<div class="progress-seg ${i<filledSeg?'filled':''}"></div>`;
  document.getElementById('challengeTrack').innerHTML = html;
  document.getElementById('challengeText').textContent = `${current} / ${challengeTarget} video — devam et 💪`;
  document.getElementById('footChallenge').textContent = `${current} / ${challengeTarget}`;
}
document.getElementById('editGoalTarget').onclick = async ()=>{
  const val = prompt('Hedef video sayısı:', challengeTarget);
  if(val===null || isNaN(Number(val))) return;
  challengeTarget = Number(val);
  try{ await storageSet('yt-goal-target', String(challengeTarget), false); }catch(e){}
  renderChallenge(Number((document.getElementById('statVideos').textContent||'0').replace(/\D/g,''))||0);
};

/* ---------- Bu Ayki Hedefler ---------- */
let goals = [
  {name:'Shorts', current:0, target:12},
  {name:'Uzun Video', current:0, target:4},
  {name:'İzlenme Saati', current:0, target:4000},
];
async function loadGoals(){
  try{
    const r = await storageGet('yt-goals', false);
    if(r && r.value) goals = JSON.parse(r.value);
  }catch(e){}
  renderGoals();
}
async function saveGoals(){ try{ await storageSet('yt-goals', JSON.stringify(goals), false); }catch(e){} }
function renderGoals(){
  document.getElementById('goalsBody').innerHTML = goals.map((g,i)=>`
    <div class="goal-row">
      <div class="goal-top"><span class="goal-name">${escapeHtml(g.name)}</span><span class="goal-nums">${fmt(g.current)} / ${fmt(g.target)}</span></div>
      <div class="bar-track"><div class="bar-fill" style="width:${Math.min(100,(g.current/g.target)*100)}%"></div></div>
    </div>
  `).join('');
  document.querySelectorAll('#goalsBody .goal-row').forEach((row,i)=>{
    row.onclick = async ()=>{
      const g = goals[i];
      const val = prompt(`"${g.name}" için mevcut / hedef (örn: 8/12):`, `${g.current}/${g.target}`);
      if(val===null) return;
      const parts = val.split('/').map(s=>Number(s.trim()));
      if(parts.length===2 && !isNaN(parts[0]) && !isNaN(parts[1])){
        g.current=parts[0]; g.target=parts[1];
        await saveGoals(); renderGoals();
      }
    };
  });
}

/* ---------- Bugünkü Görevler ---------- */
let tasks = [];
async function loadTasks(){
  try{
    const r = await storageGet('yt-tasks', false);
    if(r && r.value) tasks = JSON.parse(r.value);
  }catch(e){}
  renderTasks();
}
async function saveTasks(){ try{ await storageSet('yt-tasks', JSON.stringify(tasks), false); }catch(e){} }
function renderTasks(){
  const doneCount = tasks.filter(t=>t.done).length;
  document.getElementById('taskCountLbl').textContent = tasks.length ? `${doneCount}/${tasks.length}` : '';
  document.getElementById('tasksBody').innerHTML = tasks.length ? tasks.map(t=>`
    <div class="task-row">
      <div class="task-check ${t.done?'done':''}" data-id="${t.id}">${t.done?'✓':''}</div>
      <div class="task-text ${t.done?'done':''}">${escapeHtml(t.text)}</div>
      <button class="task-del" data-id="${t.id}">✕</button>
    </div>
  `).join('') : '<div class="empty">Bugün için görev eklenmedi.</div>';

  document.querySelectorAll('.task-check').forEach(el=>{
    el.onclick = async ()=>{
      const t = tasks.find(x=>x.id===el.dataset.id);
      t.done = !t.done; await saveTasks(); renderTasks();
    };
  });
  document.querySelectorAll('.task-del').forEach(el=>{
    el.onclick = async ()=>{
      tasks = tasks.filter(x=>x.id!==el.dataset.id);
      await saveTasks(); renderTasks();
    };
  });
}
document.getElementById('taskAddBtn').onclick = async ()=>{
  const input = document.getElementById('taskInput');
  const text = input.value.trim();
  if(!text) return;
  tasks.push({id:Date.now().toString(), text, done:false});
  input.value=''; await saveTasks(); renderTasks();
};
document.getElementById('taskInput').addEventListener('keydown', e=>{ if(e.key==='Enter') document.getElementById('taskAddBtn').click(); });

/* ---------- Takvim ---------- */
let calDate = new Date();
let calPlans = {};
async function loadCalPlans(){
  try{
    const r = await storageGet('yt-calendar', false);
    if(r && r.value) calPlans = JSON.parse(r.value);
  }catch(e){}
  renderCalendar(); renderMiniWeek();
}
async function saveCalPlans(){ try{ await storageSet('yt-calendar', JSON.stringify(calPlans), false); }catch(e){} }

function renderCalendar(){
  const y=calDate.getFullYear(), m=calDate.getMonth();
  const monthNames=['Ocak','Şubat','Mart','Nisan','Mayıs','Haziran','Temmuz','Ağustos','Eylül','Ekim','Kasım','Aralık'];
  document.getElementById('calMonthLabel').textContent = `${monthNames[m]} ${y}`;
  const firstDay = new Date(y,m,1);
  const startOffset = (firstDay.getDay()+6)%7;
  const daysInMonth = new Date(y,m+1,0).getDate();
  const todayStr = new Date().toISOString().slice(0,10);
  let html = ['Pt','Sa','Ça','Pe','Cu','Ct','Pz'].map(d=>`<div class="cal-day-label">${d}</div>`).join('');
  for(let i=0;i<startOffset;i++) html += `<div class="cal-cell muted"></div>`;
  for(let d=1; d<=daysInMonth; d++){
    const dateStr = `${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
    const plan = calPlans[dateStr];
    html += `<div class="cal-cell ${dateStr===todayStr?'today':''}" data-date="${dateStr}">
      <div>${d}</div>${plan?`<div class="cal-plan" title="${escapeHtml(plan)}">${escapeHtml(plan)}</div>`:''}
    </div>`;
  }
  document.getElementById('calGrid').innerHTML = html;
  document.querySelectorAll('#calGrid .cal-cell[data-date]').forEach(cell=>{
    cell.onclick = async ()=>{
      const date = cell.dataset.date;
      const val = prompt(`${date} için planlanan video başlığı:`, calPlans[date]||'');
      if(val===null) return;
      if(val.trim()==='') delete calPlans[date]; else calPlans[date]=val.trim();
      await saveCalPlans(); renderCalendar(); renderMiniWeek();
    };
  });
}
document.getElementById('calPrev').onclick = ()=>{ calDate.setMonth(calDate.getMonth()-1); renderCalendar(); };
document.getElementById('calNext').onclick = ()=>{ calDate.setMonth(calDate.getMonth()+1); renderCalendar(); };

function renderMiniWeek(){
  const today = new Date();
  const dow = (today.getDay()+6)%7;
  const monday = new Date(today); monday.setDate(today.getDate()-dow);
  const labels=['PZT','SAL','ÇAR','PER','CUM','CMT','PAZ'];
  let gridHtml='', plansHtml='';
  const todayStr = today.toISOString().slice(0,10);
  const weekDates=[];
  for(let i=0;i<7;i++){
    const d = new Date(monday); d.setDate(monday.getDate()+i);
    const dateStr = d.toISOString().slice(0,10);
    weekDates.push(dateStr);
    const plan = calPlans[dateStr];
    gridHtml += `<div class="mini-day-label">${labels[i]}</div>`;
  }
  let numsHtml = weekDates.map(dateStr=>{
    const d = new Date(dateStr);
    const plan = calPlans[dateStr];
    return `<div class="mini-day ${dateStr===todayStr?'today':''} ${plan&&dateStr!==todayStr?'has-plan':''}">${d.getDate()}</div>`;
  }).join('');
  document.getElementById('miniWeekGrid').innerHTML = gridHtml + numsHtml;

  const withPlans = weekDates.filter(d=>calPlans[d]);
  plansHtml = withPlans.length ? withPlans.map(d=>`
    <div class="plan-item"><span class="plan-day">${d.slice(8,10)}/${d.slice(5,7)}</span><span>${escapeHtml(calPlans[d])}</span></div>
  `).join('') : '<div class="empty">Bu hafta plan yok.</div>';
  document.getElementById('miniWeekPlans').innerHTML = plansHtml;
}

/* ---------- Pipeline (Fikir Havuzu) ---------- */
let pipeline = {};
PIPELINE_STAGES.forEach(s=> pipeline[s.key]=[]);

async function loadPipeline(){
  try{
    const r = await storageGet('yt-pipeline', false);
    if(r && r.value){
      pipeline = JSON.parse(r.value);
      PIPELINE_STAGES.forEach(s=>{ if(!pipeline[s.key]) pipeline[s.key]=[]; });
    } else {
      // eski fikir listesini taşı
      try{
        const old = await storageGet('yt-ideas', false);
        if(old && old.value){
          const oldIdeas = JSON.parse(old.value);
          pipeline.fikir = oldIdeas.map(i=>({id:i.id, text:i.text}));
          await savePipeline();
        }
      }catch(e){}
    }
  }catch(e){}
  renderPipeline();
}
async function savePipeline(){ try{ await storageSet('yt-pipeline', JSON.stringify(pipeline), false); }catch(e){} }

function pipelineColHtml(stage, condensed){
  const cards = pipeline[stage.key] || [];
  const idx = PIPELINE_STAGES.findIndex(s=>s.key===stage.key);
  const nextStage = PIPELINE_STAGES[idx+1];
  const shown = condensed ? cards.slice(0,3) : cards;
  return `
    <div class="pl-col">
      <div class="pl-col-head">
        <div class="pl-col-title"><span class="dot" style="background:${stage.color};width:8px;height:8px;border-radius:50%;display:inline-block;"></span>${stage.label}</div>
        <span class="pl-count">${cards.length}</span>
      </div>
      ${shown.map(c=>`
        <div class="pl-card">
          <div>${escapeHtml(c.text)}</div>
          <div class="pl-card-actions">
            ${nextStage ? `<button class="pl-move" data-id="${c.id}" data-from="${stage.key}" data-to="${nextStage.key}">${nextStage.label} →</button>` : '<span></span>'}
            <button class="pl-del" data-id="${c.id}" data-from="${stage.key}">✕</button>
          </div>
        </div>
      `).join('')}
      ${!condensed ? `<button class="pl-add" data-stage="${stage.key}">+ Kart ekle</button>` : ''}
    </div>
  `;
}

function renderPipeline(){
  document.getElementById('dashPipeline').innerHTML = PIPELINE_STAGES.map(s=>pipelineColHtml(s,true)).join('');
  document.getElementById('fullPipeline').innerHTML = PIPELINE_STAGES.map(s=>pipelineColHtml(s,false)).join('');
  attachPipelineEvents();
}
function attachPipelineEvents(){
  document.querySelectorAll('.pl-move').forEach(btn=>{
    btn.onclick = async ()=>{
      const {id, from, to} = btn.dataset;
      const card = pipeline[from].find(c=>c.id===id);
      if(!card) return;
      pipeline[from] = pipeline[from].filter(c=>c.id!==id);
      pipeline[to].push(card);
      await savePipeline(); renderPipeline();
    };
  });
  document.querySelectorAll('.pl-del').forEach(btn=>{
    btn.onclick = async ()=>{
      const {id, from} = btn.dataset;
      pipeline[from] = pipeline[from].filter(c=>c.id!==id);
      await savePipeline(); renderPipeline();
    };
  });
  document.querySelectorAll('.pl-add').forEach(btn=>{
    btn.onclick = async ()=>{
      const text = prompt('Yeni kart başlığı:');
      if(!text || !text.trim()) return;
      pipeline[btn.dataset.stage].push({id:Date.now().toString(), text:text.trim()});
      await savePipeline(); renderPipeline();
    };
  });
}

/* ---------- Sponsorlar ---------- */
let sponsors = [];
async function loadSponsors(){
  try{ const r = await storageGet('yt-sponsors', false); if(r&&r.value) sponsors=JSON.parse(r.value); }catch(e){}
  renderSponsors();
}
async function saveSponsors(){ try{ await storageSet('yt-sponsors', JSON.stringify(sponsors), false); }catch(e){} }
function renderSponsors(){
  const tbody = document.querySelector('#sponsorTable tbody');
  if(!sponsors.length){ tbody.innerHTML = `<tr><td colspan="3"><div class="empty">Henüz sponsor eklenmedi.</div></td></tr>`; return; }
  tbody.innerHTML = `<tr><th>Marka</th><th>Durum</th><th></th></tr>` + sponsors.map(s=>`
    <tr>
      <td>${escapeHtml(s.name)}</td>
      <td><select class="mini-select" data-id="${s.id}">
        ${['Görüşülüyor','Anlaşıldı','Yayında'].map(o=>`<option value="${o}" ${o===s.status?'selected':''}>${o}</option>`).join('')}
      </select></td>
      <td><button class="pl-del" data-del="${s.id}">✕</button></td>
    </tr>
  `).join('');
  tbody.querySelectorAll('select').forEach(sel=>{
    sel.onchange = async ()=>{
      const s = sponsors.find(x=>x.id===sel.dataset.id); s.status = sel.value;
      await saveSponsors();
    };
  });
  tbody.querySelectorAll('[data-del]').forEach(btn=>{
    btn.onclick = async ()=>{ sponsors = sponsors.filter(x=>x.id!==btn.dataset.del); await saveSponsors(); renderSponsors(); };
  });
}
document.getElementById('sponsorAddBtn').onclick = async ()=>{
  const input = document.getElementById('sponsorInput');
  const name = input.value.trim();
  if(!name) return;
  sponsors.push({id:Date.now().toString(), name, status:document.getElementById('sponsorStatus').value});
  input.value=''; await saveSponsors(); renderSponsors();
};

/* ---------- Wishlist ---------- */
let wishlist = [];
async function loadWishlist(){
  try{ const r = await storageGet('yt-wishlist', false); if(r&&r.value) wishlist=JSON.parse(r.value); }catch(e){}
  renderWishlist();
}
async function saveWishlist(){ try{ await storageSet('yt-wishlist', JSON.stringify(wishlist), false); }catch(e){} }
function renderWishlist(){
  const tbody = document.querySelector('#wishTable tbody');
  if(!wishlist.length){ tbody.innerHTML = `<tr><td><div class="empty">Wishlist boş.</div></td></tr>`; return; }
  tbody.innerHTML = `<tr><th>Ürün</th><th></th></tr>` + wishlist.map(w=>`
    <tr><td>${escapeHtml(w.text)}</td><td><button class="pl-del" data-del="${w.id}">✕</button></td></tr>
  `).join('');
  tbody.querySelectorAll('[data-del]').forEach(btn=>{
    btn.onclick = async ()=>{ wishlist = wishlist.filter(x=>x.id!==btn.dataset.del); await saveWishlist(); renderWishlist(); };
  });
}
document.getElementById('wishAddBtn').onclick = async ()=>{
  const input = document.getElementById('wishInput');
  const text = input.value.trim();
  if(!text) return;
  wishlist.push({id:Date.now().toString(), text});
  input.value=''; await saveWishlist(); renderWishlist();
};

/* ---------- Fikir Asistanı (AI Chat) ---------- */
let chatHistory = [];

function chatContextSummary(){
  const chanName = document.getElementById('brandName').textContent;
  const subs = document.getElementById('statSubs').textContent;
  const views = document.getElementById('statViews').textContent;
  const videoCount = document.getElementById('statVideos').textContent;
  const recentTitles = lastVideos.slice(0,8).map(v=>v.snippet.title).join(' | ') || 'henüz veri yok';
  return `Kanal adı: ${chanName}. Abone: ${subs}. Toplam izlenme: ${views}. Video sayısı: ${videoCount}. Son video başlıkları: ${recentTitles}.`;
}

function appendMsg(role, html, opts){
  opts = opts || {};
  const wrap = document.getElementById('chatMessages');
  const div = document.createElement('div');
  div.className = 'msg ' + role + (opts.loading ? ' loading' : '');
  div.innerHTML = html;
  if(opts.id) div.id = opts.id;
  wrap.appendChild(div);
  wrap.scrollTop = wrap.scrollHeight;
  return div;
}

function renderAiMessage(text){
  const lines = text.split('\n');
  let html = '';
  lines.forEach(line=>{
    const trimmed = line.trim();
    const m = trimmed.match(/^(?:[-*]|\d+[\.\)])\s+(.+)/);
    if(m){
      const ideaText = m[1].replace(/\*\*/g,'').trim();
      const safe = escapeHtml(ideaText).replace(/'/g,'&#39;');
      html += `<div style="margin:5px 0;">• ${escapeHtml(ideaText)} <button class="msg-add" data-idea="${safe}">+ Panoya Ekle</button></div>`;
    } else if(trimmed){
      html += `<div style="margin:4px 0;">${escapeHtml(trimmed)}</div>`;
    }
  });
  return html || escapeHtml(text);
}

async function saveChatHistory(){ try{ await storageSet('yt-chat-history', JSON.stringify(chatHistory), false); }catch(e){} }
async function loadChatHistory(){
  try{
    const r = await storageGet('yt-chat-history', false);
    if(r && r.value){
      chatHistory = JSON.parse(r.value);
      chatHistory.forEach(m=>{
        if(m.role==='user') appendMsg('user', escapeHtml(m.content));
        else { const el = appendMsg('ai', renderAiMessage(m.content)); attachIdeaAddButtons(el); }
      });
    }
  }catch(e){}
}

async function sendChat(){
  const input = document.getElementById('chatInput');
  const text = input.value.trim();
  if(!text) return;
  input.value = '';
  appendMsg('user', escapeHtml(text));
  chatHistory.push({role:'user', content:text});
  await saveChatHistory();
  const loadingEl = appendMsg('ai', 'Düşünüyor...', {loading:true, id:'chat-loading'});

  try{
    const systemPrompt = `Sen bir YouTube içerik stratejisti ve fikir ortağısın. Kullanıcının kanalıyla ilgili şu bilgiler var: ${chatContextSummary()} Kısa, somut ve uygulanabilir öneriler ver. Video fikri önerirken her fikri ayrı bir madde (- ile başlayan satır) olarak yaz ki kullanıcı tek tek panosuna ekleyebilsin. Türkçe cevap ver.`;
    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify({
        model: "claude-sonnet-4-6",
        max_tokens: 1000,
        system: systemPrompt,
        messages: chatHistory
      })
    });
    const data = await response.json();
    const replyText = (data.content||[]).map(b=>b.text||'').join('\n').trim() || 'Bir cevap alınamadı.';
    chatHistory.push({role:'assistant', content: replyText});
    await saveChatHistory();
    loadingEl.remove();
    const aiDiv = appendMsg('ai', renderAiMessage(replyText));
    attachIdeaAddButtons(aiDiv);
  }catch(err){
    loadingEl.remove();
    appendMsg('ai', 'Bağlantı hatası: ' + err.message);
  }
}

function attachIdeaAddButtons(container){
  container.querySelectorAll('.msg-add').forEach(btn=>{
    btn.onclick = async ()=>{
      const idea = btn.dataset.idea;
      pipeline.fikir.push({id:Date.now().toString()+Math.random().toString(36).slice(2,6), text:idea});
      await savePipeline(); renderPipeline();
      btn.textContent = '✓ Eklendi';
      btn.disabled = true;
    };
  });
}

document.getElementById('chatSendBtn').onclick = sendChat;
document.getElementById('chatInput').addEventListener('keydown', e=>{ if(e.key==='Enter') sendChat(); });
document.getElementById('chatClearBtn').onclick = async ()=>{
  chatHistory = [];
  document.getElementById('chatMessages').innerHTML = '';
  await saveChatHistory();
};

/* ---------- Analytics OAuth ---------- */
let oauthClientId = '';
let accessToken = '';
let tokenClient = null;

async function loadOauthSettings(){
  try{
    const r = await storageGet('yt-oauth-client', false);
    if(r && r.value){ oauthClientId = r.value; document.getElementById('oauthClientIdInput').value = oauthClientId; }
  }catch(e){}
}
document.getElementById('saveOauthBtn').onclick = async ()=>{
  const val = document.getElementById('oauthClientIdInput').value.trim();
  const st = document.getElementById('oauthStatus');
  if(!val){ st.textContent='Client ID gerekli.'; st.className='status-msg err'; return; }
  oauthClientId = val;
  try{ await storageSet('yt-oauth-client', oauthClientId, false); }catch(e){}
  st.textContent='Kaydedildi. Analytics sayfasından bağlanabilirsin.'; st.className='status-msg';
};

document.getElementById('analyticsConnectBtn').onclick = ()=>{
  const statusEl = document.getElementById('analyticsAuthStatus');
  if(location.protocol === 'file:'){
    statusEl.textContent = 'OAuth file:// üzerinden çalışmaz. Bu sayfayı http/https bir adreste barındırman gerekiyor (ör. GitHub Pages).';
    return;
  }
  if(!oauthClientId){
    statusEl.textContent = 'Önce Ayarlar sayfasından OAuth Client ID gir ve kaydet.';
    return;
  }
  if(typeof google === 'undefined'){
    statusEl.textContent = 'Google giriş kütüphanesi yüklenemedi. İnternet bağlantını kontrol et.';
    return;
  }
  if(!tokenClient){
    tokenClient = google.accounts.oauth2.initTokenClient({
      client_id: oauthClientId,
      scope: 'https://www.googleapis.com/auth/yt-analytics.readonly',
      callback: async (resp)=>{
        if(resp.error){ statusEl.textContent = 'Yetkilendirme hatası: ' + resp.error; return; }
        accessToken = resp.access_token;
        statusEl.textContent = 'Bağlandı ✓ — veriler yükleniyor...';
        await fetchAnalyticsData();
      }
    });
  }
  tokenClient.requestAccessToken();
};

async function fetchAnalyticsData(){
  const statusEl = document.getElementById('analyticsAuthStatus');
  try{
    const end = new Date();
    const start = new Date(); start.setDate(end.getDate()-30);
    const fmtDate = d => d.toISOString().slice(0,10);
    const url = `https://youtubeanalytics.googleapis.com/v2/reports?ids=channel==MINE&startDate=${fmtDate(start)}&endDate=${fmtDate(end)}&metrics=views,estimatedMinutesWatched,subscribersGained,averageViewDuration`;
    const res = await fetch(url, { headers: { Authorization: 'Bearer ' + accessToken } });
    const data = await res.json();
    if(data.error) throw new Error(data.error.message);
    const row = (data.rows && data.rows[0]) || [0,0,0,0];
    document.getElementById('anViews30').textContent = fmt(row[0]);
    document.getElementById('anMinutes30').textContent = fmt(row[1]);
    document.getElementById('anSubsGained').textContent = fmt(row[2]);
    document.getElementById('anAvgDuration').textContent = Math.round(row[3]) + ' sn';
    document.getElementById('analyticsRealStats').style.display = 'grid';
    statusEl.textContent = 'Bağlı ✓ — son 30 gün verileri gösteriliyor.';

    const trafficUrl = `https://youtubeanalytics.googleapis.com/v2/reports?ids=channel==MINE&startDate=${fmtDate(start)}&endDate=${fmtDate(end)}&metrics=views&dimensions=insightTrafficSourceType&sort=-views`;
    const trafficRes = await fetch(trafficUrl, { headers: { Authorization: 'Bearer ' + accessToken } });
    const trafficData = await trafficRes.json();
    if(trafficData.rows){
      const max = Math.max(...trafficData.rows.map(r=>r[1]), 1);
      document.getElementById('analyticsTrafficBody').innerHTML = `
        <h3 style="font-size:13px;margin-bottom:10px;">Trafik Kaynakları (Son 30 Gün)</h3>
        ${trafficData.rows.map(r=>`
          <div class="goal-row">
            <div class="goal-top"><span class="goal-name">${escapeHtml(r[0])}</span><span class="goal-nums">${fmt(r[1])}</span></div>
            <div class="bar-track"><div class="bar-fill" style="width:${(r[1]/max)*100}%"></div></div>
          </div>
        `).join('')}`;
    }
  }catch(err){
    statusEl.textContent = 'Veri alınamadı: ' + err.message;
  }
}

/* ---------- Init ---------- */
loadOauthSettings();
loadChatHistory();
loadSettings();
loadChallenge();
loadGoals();
loadTasks();
loadCalPlans();
loadPipeline();
loadSponsors();
loadWishlist();
</script>
</body>
</html>
